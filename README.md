These tools utilise Sun Grid Engine  commands to

 * View the resource usage for a running job (ViewJobHosts.py);
 * View the resource usage for a resource allocation project (ViewProjectUsage.py).

Paths are hard-coded for ViewProjectUsage.py.
